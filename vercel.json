{
    "buildCommand": "ng build --configuration=production",
    "outputDirectory": "dist/interactive-map/browser",
    "rewrites": [
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}